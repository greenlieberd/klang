Analyse how BENCH is being used and where it could be improved. Do the following in order:

## 1. Usage snapshot

Read `state/active.json`. Read `vault/00-index.md`. List every folder in `projects/` and for each read the loop.md status line.

Report:
- How many projects exist, what stage each is in
- Total loop entries across all active projects
- Last session date
- Open questions across all projects

## 2. Knowledge gaps

Check for:
- Anything marked `[NEEDS VERIFICATION]` in any project file or skill file
- Files in `skills/_drafts/` that haven't been promoted
- Empty vault categories (sections in vault/00-index.md with no entries)
- Topics that appear in loop.md or research.md files but have no matching skill

Report these as a prioritised list: **what's missing and why it matters**.

## 3. Data gaps (what raw/ needs)

For the active project, check what components are in materials.md and cross-reference against vault/components/. For any component with no vault note, flag it as: "Drop the [PartName] datasheet into raw/ — it will improve future diagnoses."

## 4. Improvement suggestions

Based on what you've seen:
- What skill files would be most valuable to add next?
- What vault categories are thin that will matter as projects progress?
- Is there anything in the loop history that suggests a skill gap (recurring uncertainty, repeated `[NEEDS VERIFICATION]` on the same topic)?

Format the whole response as a short briefing — bullets, not prose. Actionable, not observational.
